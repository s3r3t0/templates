\subsection{Scope}

The scope of the static application test was limited to the scope given below.

\begin{longtable}{ r l }
  \toprule
  Name of the target & ((( target.data.name | tex ))) \\
  Source code provider & %
    ((( target.data.code_origin | tex ))) \\
  Source code origin & %
    ((( target.data.code_origin_name | tex ))) \\
  \bottomrule
\end{longtable}

((* if target.data.code_integrity | length > 0 *))
  Source code integrity (SHA-256):
  \begin{description}
    ((* for k,v in target.data.code_integrity.items() *))
      \item[\nolinkurl{ (((- k -))) }] \nolinkurl{ (((- v -))) }
    ((* endfor *))
  \end{description}
((* endif *))

